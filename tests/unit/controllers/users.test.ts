// import * as UsersController from '../../../src/controllers/users';

afterEach(() => {
	jest.clearAllMocks();
});

describe('Auth Controller', () => {
	describe('registerUser', () => {
		test('It should register a new user', async () => {
			// const payload = {
			// 	email: 'test@test.com',
			// 	password: 'password',
			// 	confirmPassword: 'password',
			// };
			// const user = await UsersController.registerUser(payload);
		});
	});
});
